import React from 'react';

const About = (props) => (
    <div className="main-content">
        <h2>{props.title}</h2>
        <p>The front End Course directory lists many of the courses we teach on HTML...</p>
    </div>

);

export default About;